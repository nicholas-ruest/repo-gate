//! RepoGate repository ingestion: cloning, file-tree walking, language detection, SBOM extraction.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Workspace compiles and links correctly.
    }
}
