//! RepoGate report: template rendering for JSON canonical output, Markdown, and optional PDF.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Workspace compiles and links correctly.
    }
}
